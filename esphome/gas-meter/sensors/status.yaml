# ============================================================================
# Status Binary Sensor
# ============================================================================
# Shows if device is online or offline
# Uses name substitution: name_status

binary_sensor:
  # Status Sensor - Shows if device is online or offline
  - platform: status
    id: status_sensor
    name: "${name_status}"
